use crate::prelude::*;

// A component that represents the player in the game
// [TODO]: Consider impact of having multiple players
#[derive(Component)]
pub struct Player;

// Opponents
#[derive(Component)]
pub struct Opponent;